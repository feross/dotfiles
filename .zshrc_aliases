# General
alias l='ls -lha'

# Git
# (more aliases in ~/.oh-my-zsh/plugins/git)
alias gd='git diff'
alias gs='git status'

# Server-specific shortcuts
if [[ -f "/etc/hostname" ]] ; then
  # Admin stuff
  alias uu='sudo aptitude update && sudo aptitude upgrade'
  alias ss='sudo service'
  alias apt='sudo aptitude'

  # Folder stuff
  alias www='cd ~/www'
  alias log='cd ~/www/log'
  alias apache2='cd /etc/apache2/'
  alias nginx='cd /etc/nginx/'
fi

# Mac-specific shortcuts
if [[ ! -f "/etc/hostname" ]] ; then
  # Alias `git` to `hub` (https://github.com/defunkt/hub)
  eval "$(hub alias -s)"

  alias s='subl'
  alias mongod='/usr/local/Cellar/mongodb/2.2.0-x86_64/bin/mongod'
fi
